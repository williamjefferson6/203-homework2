float averageValueInFile(string s){
    ifstream fin;
    fin.open(s);
    float x = 0;
    float sum = 0;
    int c = 0;
    while(fin >> x){
        sum += x;
        c++;
    }
    if (c == 0){
        return 0.0;
    }
    fin.close();
    return sum / c;
}
